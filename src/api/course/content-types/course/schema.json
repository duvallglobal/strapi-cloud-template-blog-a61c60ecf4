{
  "kind": "collectionType",
  "collectionName": "courses",
  "info": {
    "singularName": "course",
    "pluralName": "courses",
    "displayName": "Course",
    "description": "Educational courses with sections and lessons"
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "title": {
      "type": "string",
      "required": true
    },
    "slug": {
      "type": "uid",
      "targetField": "title"
    },
    "description": {
      "type": "text",
      "required": true
    },
    "sections": {
      "type": "json"
    },
    "tags": {
      "type": "json"
    },
    "accessLevel": {
      "type": "enumeration",
      "enum": ["free", "subscriber", "premium"],
      "default": "free"
    },
    "media": {
      "type": "media",
      "multiple": true,
      "required": false,
      "allowedTypes": ["images", "files", "videos"]
    },
    "duration": {
      "type": "string"
    },
    "skillLevel": {
      "type": "enumeration",
      "enum": ["beginner", "intermediate", "advanced"]
    },
    "status": {
      "type": "enumeration",
      "enum": ["draft", "published", "archived"],
      "default": "draft"
    },
    "views": {
      "type": "integer",
      "default": 0
    }
  }
}

